ANTHROPIC_API_KEY=sk-ant-api03-...
FLASK_SECRET_KEY=change-me-to-a-long-random-string
CLERK_JWKS_URL=https://your-instance.clerk.accounts.dev/.well-known/jwks.json

# Optional: use PostgreSQL instead of SQLite
# DATABASE_URL=postgresql://user:password@host:5432/soc_analyst

# Optional: custom SQLite path (set automatically by docker-compose)
# DB_PATH=/app/data/soc_analyst.db

# Optional: restrict CORS origins (set automatically by docker-compose)
# CORS_ORIGINS=http://localhost
